import FirstParameters from "./first-parameters";
export default function FirstParameter({ iterable, defaults = null, value, }) {
    return FirstParameters(iterable || value, defaults);
}
//# sourceMappingURL=first-parameter.js.map